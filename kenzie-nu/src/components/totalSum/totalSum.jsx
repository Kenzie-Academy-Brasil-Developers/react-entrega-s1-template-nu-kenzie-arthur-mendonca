function TotalSum() {
  return (
    <>
      <section>
        {console.log()}
        {Array.from().map((e) => (
          <p>{e}</p>
        ))}
      </section>
    </>
  );
}

export default TotalSum;
